server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
    location /api/ {
        # O nome 'orchestrator' deve coincidir com o service no docker-compose.yml
        proxy_pass http://orchestrator:8000/;
    }
}
